export enum AccrualMethod {
    MONTHLY = 'MONTHLY',
    ANNUAL_GRANT = 'ANNUAL_GRANT',
    NONE = 'NONE',
}

export enum DayType {
    FULL = 'FULL',
    HALF = 'HALF',
}

export enum LeaveRequestStatus {
    PENDING = 'PENDING',
    APPROVED = 'APPROVED',
    REJECTED = 'REJECTED',
    CANCELLED = 'CANCELLED',
}
